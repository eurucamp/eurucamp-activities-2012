defaults: &defaults
  twitter:
    account: "Eactivitiestest"
    tokens:
      im_in: "#imin"
      im_out: "#imout"

development:
  <<: *defaults

test:
  twitter:
    account: "eurucamplivetest"
    tokens:
      im_in: "#imin"
      im_out: "#imout"

production:
  <<: *defaults
